---

- name: Converge
  hosts: all
  become: "{{ molecule['converge']['become'] | default('yes') }}"
  tasks:
    - name: Include role 'ansible-role-nfs'
      include_role:
        name: ansible-role-nfs
